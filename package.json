{
  "name": "check-security",
  "version": "1.0.0",
  "description": "An MCP server that scans files and directories for sensitive information such as API keys, tokens, passwords, and credentials to prevent accidental exposure in public repositories.",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.9.3"
  }
}